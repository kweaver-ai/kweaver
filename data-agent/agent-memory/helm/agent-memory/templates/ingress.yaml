{{- if .Values.service.agentMemory.ingress.enabled -}}
{{ if .Values.service.agentMemory.ingress.interface }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.moduleName }}-ingress
  namespace: {{ .Values.namespace }}
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "500M"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
{{- if .Values.service.ingressclassname }}
  ingressClassName: "{{.Values.service.ingressclassname}}"
{{- end }}
  rules:
  - http:
      paths:
        {{- range .Values.service.agentMemory.ingress.interface.path }}
        - path: {{ . }}
          pathType: ImplementationSpecific
          backend:
            service:
              name: {{ $.Values.moduleName }}
              port:
                number: {{ $.Values.service.agentMemory.port }}
        {{- end }}
{{ end }}
{{- end }}
