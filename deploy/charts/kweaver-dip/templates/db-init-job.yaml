{{- if .Values.dbInit.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}-db-init
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: {{ .Values.dbInit.backoffLimit | default 3 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Chart.Name }}-db-init
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      restartPolicy: Never
      initContainers:
        - name: wait-for-db
          image: busybox:1.36
          command: ['sh', '-c']
          args:
            - |
              echo "Waiting for MariaDB at {{ .Values.depServices.rds.host }}:{{ .Values.depServices.rds.port }}..."
              until nc -z {{ .Values.depServices.rds.host }} {{ .Values.depServices.rds.port }}; do
                echo "  MariaDB not ready, retrying in 5s..."
                sleep 5
              done
              echo "MariaDB is reachable."
      containers:
        - name: migrate
          image: "{{ if .Values.global.imageRegistry }}{{ .Values.global.imageRegistry }}/{{ end }}{{ .Values.dbInit.image.repository }}:{{ .Values.dbInit.image.tag }}"
          imagePullPolicy: {{ .Values.dbInit.image.pullPolicy | default "IfNotPresent" }}
          env:
            - name: DB_HOST
              value: {{ .Values.depServices.rds.host | quote }}
            - name: DB_PORT
              value: {{ .Values.depServices.rds.port | quote }}
            - name: DB_USER
              value: {{ .Values.depServices.rds.user | quote }}
            - name: DB_PASSWORD
              value: {{ .Values.depServices.rds.password | quote }}
            - name: DB_NAME
              value: {{ .Values.depServices.rds.database | default "adp" | quote }}
            - name: DB_TYPE
              value: {{ .Values.depServices.rds.type | default "MariaDB" | quote }}
            - name: MIGRATION_TARGET
              value: "dip"
{{- end }}
